describe('Header', () => {
  test('Test Title', () => {
    expect('Hola Mundo').toMatch(/Mundo/);
  });
  test('Test Logo', () => {
    expect('Oscar').toMatch(/Oscar/);
  });
  test('Test Nav', () => {
    expect('Oscar').toMatch(/Oscar/);
  });
});

